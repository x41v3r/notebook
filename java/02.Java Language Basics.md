# 1 Creating variables and naming them

## 1.1 Variables

The Java programming language defines the following kinds of variables:

* **Instance Variables (Non-Static Fields)**
* **Class Variables (Static Fields)**: The fields which are declared with the `static` modifier. 
* **Local Variables**
* **Parameters**

## 1.2 Naming variables

The rules and conventions for naming our variables can be summarized as follows:

1. Variable names are **case-sensitive**.
2. A variable's name can be any legal identifier — an unlimited-length sequence of Unicode letters and digits, beginning with a letter, the dollar sign `$`, or the underscore character `_`. **A digit is not allowed to be the first letter.**
3. Subsequent characters may be letters, digits, dollar signs, or underscore characters. 
4. If the name we choose consists of only one word, **spell that word in all lowercase letters**. If it consists of more than one word, **capitalize the first letter of each subsequent word**.
5. If our variable stores a constant value, such as `static final int NUM_GEARS = 6`, the convention changes slightly, capitalizing every letter and separating subsequent words with the underscore character `_`. 

# 2 Creating primitive type variables in our programs

## 2.1 Primitive types

The eight primitive data types supported by the Java programming language are:

| Type Name  | Length                    | Range                  |
|:----------:|:-------------------------:|:----------------------:|
| `byte`     | 8-bit                     | $[-128,127]$           |
| `short`    | 16-bit                    | $[-32768,32767]$       |
| `int`      | 32-bit                    | $[-2^{31},2^{31}-1]$   |
| `long`     | 64-bit                    | $[-2^{63},2^{63}-1]$   |
| `float`    | 32-bit (single-precision) | |
||||
||||
||||

2. `float`: A single-precision 32-bit IEEE 754 floating point.
3. `double`: A double-precision 64-bit IEEE 754 floating point.
4. `boolean`: This type has only two possible values: true and false. 
5. `char`: A single 16-bit Unicode character.

## 2.2 Initializing a variable with a default value

## 2.3 Creating values with literals

# 3 Creating arrays in our programs



