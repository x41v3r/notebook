# 1 Getting started with MySQL

## 1.1 What is MySQL?

&emsp;&emsp;MySQL is a robust database management system designed for managing relational databases.

## 1.2 Install MySQL

## 1.3 Connect to MySQL Server

# 2 MySQL Basics

## 2.1 Querying data

The `SELECT` statement allows us to select data from one or more tables. 

```sql
SELECT select_list
FROM table_name;
```

To use the `SELECT` statement without referencing a table.


