% this is the preamble file
% preamble here

% \documentclass[50pt,twoside,a4paper]{book}
\documentclass{article}

\usepackage{hyperref} % generate bookmarks auto
\usepackage{geometry} % to set the page margin
\usepackage{listings} % for the code blocks
\usepackage{xcolor} % to highlight the code
\usepackage{graphicx} % to instert pictures

\geometry{
	left=2.18cm,
	right=2.18cm,
	top=2.18cm,
	bottom=2.18cm
} % set the margin of the document
\lstset{
    numbers = left,   % the position of the line number is left
    frame = lrtb,     % disply the border
    commentstyle = \rmfamily\itshape % the style of comment
}% set the theme of code block

% \title{x41v3r's notebook}

% \title{\begin{center}{\Huge \textit{x41v3r's Notebook}}\\{{\itshape He just a man but he is stronger than the god. }}\end{center}}
\title{\begin{center}{\Huge \textit{x41v3r's Notebook}}\\{{\itshape As a mortal, I wish to defeat the god. }}\end{center}}
